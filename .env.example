# Galactus — Variables d'environnement
# Copier ce fichier en .env.local et compléter les valeurs.
# .env* est gitignored — ne JAMAIS commit.

# Supabase (client browser et Edge Functions)
SUPABASE_URL=https://eqlofgcravaihvfaysdb.supabase.co
SUPABASE_ANON_KEY=                  # anon key publique — visible côté client, RLS doit protéger en sprint sécu

# Claude API (Edge Functions Deno uniquement, jamais côté client)
ANTHROPIC_API_KEY=                  # https://console.anthropic.com/settings/keys
